// Mock untuk package 'server-only' di Jest environment.
// Package ini hanya berfungsi sebagai guard di Next.js server runtime —
// di Jest (Node) tidak diperlukan karena tidak ada client bundle.
export {};
